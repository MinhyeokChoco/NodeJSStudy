<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
</head>

<body>
    <div>
        게시판 홈페이지
        <% rankList.forEach((rl, index)=> { %>
            <div>
                <%= rl.id %>
                    <%= rl.name %>
            </div>
            <% }); %>
    </div>
</body>

</html>