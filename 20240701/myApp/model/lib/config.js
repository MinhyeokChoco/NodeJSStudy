const config = {
    dev: {
        username: "root",
        password: "dlalsgur12",
        database: "app",
        host: "127.0.0.1",
        dialect: "mysql"
    }
}

// show databases;
// create database [스키마 명];

module.exports = config;