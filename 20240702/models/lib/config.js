// 데이터베이스 사용자 설정 객체
const config = {
    // 프로젝트
    project: {
        username: "root",
        password: "dlalsgur12",
        database: "CRUD",
        host: "127.0.0.1",
        dialect: "mysql"
    }
}

module.exports = config;