// 데이터 베이스 사용자 설정 객체
const config = {
    project: {
        username: "root",
        password: "dlalsgur12",
        database: "project",
        host: "127.0.0.1",
        dialect: "mysql"
    }
}

module.exports = config;